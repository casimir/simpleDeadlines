<?xml version="1.0" encoding="UTF-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/act_share"
        android:icon="@drawable/ic_act_share"
        android:title="@string/act_share"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/act_group"
        android:icon="@drawable/ic_act_group"
        android:title="@string/act_group"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/act_delete"
        android:icon="@drawable/ic_act_delete"
        android:title="@string/act_delete"
        app:showAsAction="ifRoom" />
</menu>